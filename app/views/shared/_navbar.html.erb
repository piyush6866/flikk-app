<nav class="fixed top-0 left-0 right-0 z-40 bg-white border-b border-slate-100">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <% if user_signed_in? %>
        <!-- LOGGED IN: Left side - Logo with Brand/Creator dropdown + Nav Links -->
        <div class="flex items-center gap-8">
          <!-- Logo with Brand Name Dropdown (like Billo) -->
          <div class="relative" data-controller="dropdown">
            <button 
              data-action="click->dropdown#toggle" 
              data-dropdown-target="button"
              class="flex items-center gap-2.5 hover:opacity-80 transition-opacity"
            >
              <div class="w-9 h-9 bg-slate-900 rounded-lg flex items-center justify-center">
                <span class="text-white font-extrabold text-base tracking-tighter">k<span class="inline-block" style="transform: scaleX(-1);">k</span></span>
              </div>
              <span class="text-lg font-bold text-slate-900"><%= current_user.brand? ? current_user.brand_name : current_user.name %></span>
              <svg class="w-4 h-4 text-slate-400 transition-transform" data-dropdown-target="arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            
            <!-- Brand/Creator Switcher Dropdown (placeholder for future multi-brand support) -->
            <div 
              data-dropdown-target="menu"
              class="absolute left-0 mt-2 w-64 bg-white rounded-xl shadow-xl border border-slate-100 opacity-0 invisible transform scale-95 transition-all duration-200 z-50"
            >
              <div class="p-2">
                <div class="px-4 py-3 border-b border-slate-100">
                  <p class="text-xs text-slate-500 uppercase tracking-wide font-medium">Current Account</p>
                </div>
                <div class="flex items-center gap-3 px-4 py-3 bg-indigo-50 rounded-lg">
                  <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-violet-600 rounded-full flex items-center justify-center text-white font-bold">
                    <%= current_user.brand? ? current_user.brand_name&.first&.upcase : current_user.name&.first&.upcase %>
                  </div>
                  <div>
                    <p class="font-semibold text-slate-900"><%= current_user.brand? ? current_user.brand_name : current_user.name %></p>
                    <p class="text-xs text-slate-500"><%= current_user.brand? ? 'Brand Account' : 'Creator Account' %></p>
                  </div>
                  <svg class="w-5 h-5 text-indigo-600 ml-auto" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                  </svg>
                </div>
              </div>
            </div>
          </div>

          <!-- Main Navigation Links -->
          <div class="hidden md:flex items-center gap-1">
            <% if current_user.brand? %>
              <%= link_to "#", class: "px-4 py-2 text-slate-600 hover:text-slate-900 font-medium transition-colors rounded-lg hover:bg-slate-50" do %>
                Explore creators
              <% end %>
              <%= link_to brand_dashboard_path, class: "px-4 py-2 font-medium transition-colors rounded-lg hover:bg-slate-50 #{controller_name == 'dashboard' ? 'text-slate-900' : 'text-slate-600 hover:text-slate-900'}" do %>
                Manage campaigns
              <% end %>
              <span class="px-4 py-2 text-slate-400 font-medium cursor-not-allowed flex items-center gap-1.5">
                Performance hub
                <span class="px-1.5 py-0.5 text-[10px] font-semibold bg-emerald-100 text-emerald-700 rounded">New</span>
              </span>
            <% else %>
              <%= link_to creator_dashboard_path, class: "px-4 py-2 font-medium transition-colors rounded-lg hover:bg-slate-50 #{controller_name == 'dashboard' ? 'text-slate-900' : 'text-slate-600 hover:text-slate-900'}" do %>
                Dashboard
              <% end %>
              <%= link_to campaigns_path, class: "px-4 py-2 font-medium transition-colors rounded-lg hover:bg-slate-50 #{controller_name == 'campaigns' ? 'text-slate-900' : 'text-slate-600 hover:text-slate-900'}" do %>
                Browse Jobs
              <% end %>
              <span class="px-4 py-2 text-slate-400 font-medium cursor-not-allowed">
                My Applications
              </span>
            <% end %>
          </div>
        </div>

        <!-- LOGGED IN: Right side - Inbox, Wallet, Profile -->
        <div class="flex items-center gap-2">
          <% if current_user.brand? %>
            <!-- Inbox -->
            <button class="relative p-2.5 text-slate-500 hover:text-slate-900 hover:bg-slate-50 rounded-lg transition-colors" title="Inbox">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
              </svg>
              <span class="sr-only">Inbox</span>
            </button>

            <!-- Wallet -->
            <button class="flex items-center gap-2 px-3 py-2 text-slate-600 hover:text-slate-900 hover:bg-slate-50 rounded-lg transition-colors" title="Wallet">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
              </svg>
              <span class="font-medium">₹0</span>
            </button>
          <% end %>

          <!-- Profile Dropdown -->
          <div class="relative" data-controller="dropdown">
            <button 
              data-action="click->dropdown#toggle" 
              data-dropdown-target="button"
              class="flex items-center gap-2 p-1.5 rounded-full hover:bg-slate-50 transition-colors"
            >
              <div class="w-8 h-8 bg-gradient-to-br from-indigo-500 to-violet-600 rounded-full flex items-center justify-center text-white font-semibold text-sm">
                <%= current_user.brand? ? current_user.brand_name&.first&.upcase : current_user.name&.first&.upcase %>
              </div>
              <svg class="w-4 h-4 text-slate-400 transition-transform" data-dropdown-target="arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            
            <!-- Profile Dropdown Menu -->
            <div 
              data-dropdown-target="menu"
              class="absolute right-0 mt-2 w-64 bg-white rounded-xl shadow-xl border border-slate-100 opacity-0 invisible transform scale-95 transition-all duration-200 z-50"
            >
              <div class="p-2">
                <!-- User Info Header -->
                <div class="px-4 py-3 border-b border-slate-100">
                  <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-violet-600 rounded-full flex items-center justify-center text-white font-bold">
                      <%= current_user.brand? ? current_user.brand_name&.first&.upcase : current_user.name&.first&.upcase %>
                    </div>
                    <div>
                      <p class="font-semibold text-slate-900"><%= current_user.brand? ? current_user.brand_name : current_user.name %></p>
                      <p class="text-xs text-slate-500"><%= current_user.email %></p>
                    </div>
                  </div>
                </div>
                
                <!-- Menu Items -->
                <div class="py-2">
                  <% if current_user.brand? %>
                    <%= link_to brand_dashboard_path, class: "flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors" do %>
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                      </svg>
                      <span class="text-slate-700">Dashboard</span>
                    <% end %>
                    <a href="#" class="flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors">
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
                      </svg>
                      <span class="text-slate-700">Wallet</span>
                      <span class="ml-auto text-sm font-medium text-slate-500">₹0</span>
                    </a>
                    <a href="#" class="flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors">
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                      </svg>
                      <span class="text-slate-700">Settings</span>
                    </a>
                  <% else %>
                    <%= link_to creator_dashboard_path, class: "flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors" do %>
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                      </svg>
                      <span class="text-slate-700">Dashboard</span>
                    <% end %>
                    <%= link_to campaigns_path, class: "flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors" do %>
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                      </svg>
                      <span class="text-slate-700">Browse Jobs</span>
                    <% end %>
                    <a href="#" class="flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-slate-50 transition-colors">
                      <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      <span class="text-slate-700">Earnings</span>
                      <span class="ml-auto text-sm font-medium text-slate-500">₹0</span>
                    </a>
                  <% end %>
                </div>

                <!-- Divider -->
                <div class="border-t border-slate-100 my-2"></div>

                <!-- Legal Links -->
                <div class="py-1">
                  <a href="#" class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-slate-50 transition-colors">
                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                    <span class="text-slate-600 text-sm">Terms of Service</span>
                  </a>
                  <a href="#" class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-slate-50 transition-colors">
                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                    </svg>
                    <span class="text-slate-600 text-sm">Privacy Policy</span>
                  </a>
                  <a href="#" class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-slate-50 transition-colors">
                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span class="text-slate-600 text-sm">Help Center</span>
                  </a>
                </div>
                
                <!-- Sign Out -->
                <div class="border-t border-slate-100 mt-2 pt-2">
                  <%= button_to destroy_user_session_path, method: :delete, class: "w-full flex items-center gap-3 px-4 py-2.5 rounded-lg hover:bg-rose-50 transition-colors text-left" do %>
                    <svg class="w-5 h-5 text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                    </svg>
                    <span class="text-rose-600 font-medium">Sign Out</span>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% else %>
        <!-- GUEST NAVBAR -->
        <!-- Logo (Left side) -->
        <div class="flex items-center">
          <%= link_to root_path, class: "flex items-center gap-2 hover:opacity-80 transition-opacity" do %>
            <div class="w-9 h-9 bg-slate-900 rounded-lg flex items-center justify-center">
              <span class="text-white font-extrabold text-base tracking-tighter">k<span class="inline-block" style="transform: scaleX(-1);">k</span></span>
            </div>
            <span class="text-xl font-bold text-slate-900 hidden sm:block">flik<span class="inline-block" style="transform: scaleX(-1);">k</span></span>
          <% end %>
        </div>

        <!-- Guest Navigation Links (Desktop only) -->
        <div class="hidden md:flex items-center gap-8">
          <a href="#how-it-works" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">How it Works</a>
          <a href="#creators" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Creators</a>
          <a href="#features" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Features</a>
          <a href="#success-stories" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Success stories</a>
        </div>

        <!-- Guest Auth Buttons (Right side) -->
        <div class="flex items-center gap-2 sm:gap-4">
          <%= link_to "Login", new_user_session_path, class: "px-3 py-2 sm:px-4 text-slate-900 hover:text-slate-600 transition-colors font-medium text-sm sm:text-base" %>
          
          <!-- Signup Dropdown - Click-based for mobile support -->
          <div class="relative" data-controller="dropdown">
            <button 
              data-action="click->dropdown#toggle" 
              data-dropdown-target="button"
              class="inline-flex items-center gap-1 sm:gap-2 px-3 sm:px-5 py-2 sm:py-2.5 bg-slate-900 text-white font-medium rounded-full hover:bg-slate-800 transition-colors text-sm sm:text-base"
            >
              Sign up
              <svg class="w-3 h-3 sm:w-4 sm:h-4 transition-transform" data-dropdown-target="arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            
            <!-- Dropdown Menu - Positioned to stay on screen for mobile -->
            <div 
              data-dropdown-target="menu"
              class="absolute right-0 mt-2 w-64 sm:w-56 bg-white rounded-xl shadow-xl border border-slate-100 opacity-0 invisible transform scale-95 transition-all duration-200 z-50"
              style="max-width: calc(100vw - 2rem);"
            >
              <div class="p-2">
                <%= link_to new_user_registration_path(role: 'brand'), class: "flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-slate-50 transition-colors" do %>
                  <div class="w-10 h-10 bg-slate-100 rounded-lg flex items-center justify-center flex-shrink-0">
                    <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                    </svg>
                  </div>
                  <div>
                    <div class="font-semibold text-slate-900">I'm a brand</div>
                    <div class="text-sm text-slate-500">Connect with creators</div>
                  </div>
                <% end %>
                <%= link_to new_user_registration_path(role: 'creator'), class: "flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-slate-50 transition-colors" do %>
                  <div class="w-10 h-10 bg-slate-100 rounded-lg flex items-center justify-center flex-shrink-0">
                    <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                  </div>
                  <div>
                    <div class="font-semibold text-slate-900">I'm a creator</div>
                    <div class="text-sm text-slate-500">Monetize your content</div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<script>
  // Simple dropdown controller without Stimulus dependency
  function initDropdowns() {
    const dropdowns = document.querySelectorAll('[data-controller="dropdown"]');
    
    dropdowns.forEach(function(dropdown) {
      // Skip if already initialized
      if (dropdown.dataset.initialized === 'true') return;
      dropdown.dataset.initialized = 'true';
      
      const button = dropdown.querySelector('[data-dropdown-target="button"]');
      const menu = dropdown.querySelector('[data-dropdown-target="menu"]');
      const arrow = dropdown.querySelector('[data-dropdown-target="arrow"]');
      let isOpen = false;
      
      if (button && menu) {
        button.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          isOpen = !isOpen;
          
          if (isOpen) {
            menu.classList.remove('opacity-0', 'invisible', 'scale-95');
            menu.classList.add('opacity-100', 'visible', 'scale-100');
            if (arrow) arrow.classList.add('rotate-180');
          } else {
            menu.classList.add('opacity-0', 'invisible', 'scale-95');
            menu.classList.remove('opacity-100', 'visible', 'scale-100');
            if (arrow) arrow.classList.remove('rotate-180');
          }
        });
        
        // Close on click outside
        document.addEventListener('click', function(e) {
          if (!dropdown.contains(e.target) && isOpen) {
            isOpen = false;
            menu.classList.add('opacity-0', 'invisible', 'scale-95');
            menu.classList.remove('opacity-100', 'visible', 'scale-100');
            if (arrow) arrow.classList.remove('rotate-180');
          }
        });
      }
    });
  }
  
  // Initialize on page load and Turbo navigation
  document.addEventListener('DOMContentLoaded', initDropdowns);
  document.addEventListener('turbo:load', initDropdowns);
</script>
