<nav class="fixed top-0 left-0 right-0 z-40 bg-white border-b border-slate-100">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-20">
      <!-- Logo -->
      <%= link_to root_path, class: "flex items-center gap-2.5" do %>
        <div class="w-9 h-9 bg-slate-900 rounded-lg flex items-center justify-center">
          <span class="text-white font-extrabold text-base tracking-tighter">k<span class="inline-block" style="transform: scaleX(-1);">k</span></span>
        </div>
        <span class="text-[28px] font-extrabold text-slate-900 tracking-tight" style="letter-spacing: -0.02em;">flik<span class="inline-block" style="transform: scaleX(-1);">k</span></span>
      <% end %>

      <!-- Navigation Links -->
      <div class="hidden md:flex items-center gap-8">
        <a href="#how-it-works" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">How it Works</a>
        <a href="#creators" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Creators</a>
        <a href="#features" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Features</a>
        <a href="#success-stories" class="text-slate-900 hover:text-slate-600 transition-colors font-medium">Success stories</a>
      </div>

      <!-- Auth Buttons -->
      <div class="flex items-center gap-4">
        <% if user_signed_in? %>
          <% if current_user.brand? %>
            <%= link_to "Dashboard", brand_dashboard_path, class: "text-slate-600 hover:text-slate-900 transition-colors font-medium" %>
          <% else %>
            <%= link_to "Dashboard", creator_dashboard_path, class: "text-slate-600 hover:text-slate-900 transition-colors font-medium" %>
          <% end %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-4 py-2 font-medium text-slate-600 hover:text-slate-900 transition-colors" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "text-slate-900 hover:text-slate-600 transition-colors font-medium" %>
          <div class="relative group">
            <button class="inline-flex items-center gap-2 px-5 py-2.5 bg-slate-900 text-white font-medium rounded-full hover:bg-slate-800 transition-colors">
              Sign up
              <svg class="w-4 h-4 transition-transform group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            <!-- Dropdown -->
            <div class="absolute right-0 mt-2 w-56 bg-white rounded-xl shadow-xl border border-slate-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
              <div class="p-2">
                <%= link_to new_user_registration_path(role: 'brand'), class: "flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-slate-50 transition-colors" do %>
                  <div class="w-10 h-10 bg-slate-100 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                    </svg>
                  </div>
                  <div>
                    <div class="font-semibold text-slate-900">I'm a brand</div>
                    <div class="text-sm text-slate-500">Connect with creators</div>
                  </div>
                <% end %>
                <%= link_to new_user_registration_path(role: 'creator'), class: "flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-slate-50 transition-colors" do %>
                  <div class="w-10 h-10 bg-slate-100 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                  </div>
                  <div>
                    <div class="font-semibold text-slate-900">I'm a creator</div>
                    <div class="text-sm text-slate-500">Monetize your content</div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>

